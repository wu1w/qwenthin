# 控制台前端

Vite + React，页面是中文：聊天 / 控制 / 工作区 / 设置。

`q38 web` 托管的是这里的 `dist/`。改完 UI 后：

```bash
npm install
npm run build
```

`npm run dev` 把 `/api` 代理到 `http://127.0.0.1:3848`（可用 `Q38_WEB` 改）。

产品怎么用见仓库根目录 [README](../../README.md)，内部结构见 [技术说明](../../docs/architecture.md)。
